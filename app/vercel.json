{
  "buildCommand": "cd server && npm install && cd .. && npm install && npm run build",
  "outputDirectory": "dist",
  "installCommand": "cd server && npm install && cd .. && npm install",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "functions": {
    "api/index.js": {
      "memory": 256,
      "maxDuration": 10
    }
  }
}
